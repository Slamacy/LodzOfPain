using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LeaderBoard : MonoBehaviour {

	// Use this for initialization
	void Start () {
        // WebRequest.Post(string url);
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
